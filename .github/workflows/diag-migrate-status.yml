name: Diagnose migrate status (temporary)

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Check migrate status on production
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd ${{ secrets.PROJECT_DIR }} || exit 1
            echo "=== resolving the migration that was verified as fully applied ==="
            npx prisma migrate resolve --applied "20260826191229_fase5_facturacion_pagos_comunicaciones" || exit 1
            echo ""
            echo "=== prisma migrate status (after resolve) ==="
            npx prisma migrate status || true
